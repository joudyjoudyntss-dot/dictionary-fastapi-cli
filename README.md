# Dictionary API & CLI Project
A simple project built using FastAPI that acts as a backend API to manage words and their definitions. It also includes a **CLI (Command Line Interface)** application to search for and add words directly from your terminal.
# Requirements
Before running the project, you need to install the required external libraries. Run the following command in your terminal:
```bash
pip install -r requirements.txt